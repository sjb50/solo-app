import React,{Component} from 'react';

class ItemViewer extends Component{
  state={

  }

  componentDidMount() {
  }
  render(){
  return(
    <div>
      <h1>{this.item}</h1>
    </div>
  )
}}

export default ItemViewer;
